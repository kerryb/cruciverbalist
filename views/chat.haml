%h1 Chat
%ul#conversation{"data-id" => @conversation.id}
%form
  %textarea#new-message

  %script{type: "text/template", id: "message-template"}
    %span.username ${username}:
    ${content}

  %script{type: "text/template", id: "action-template"}
    %span.action ${username} ${content}
